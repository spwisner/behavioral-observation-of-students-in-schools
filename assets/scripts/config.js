'use strict';

const config = {
  apiOrigins: {
    // production: 'http://localhost:4741',
    production: 'https://behav-obs-of-students.herokuapp.com',
    // development: 'https://boss-api.herokuapp.com',
  },
};

module.exports = config;
